name: motor_deal_granada
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.5
  cached_network_image: ^3.3.1 
  url_launcher: ^6.2.1
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.0
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.3
  firebase_storage: ^12.4.5
  cloud_firestore: ^5.5.2
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  geolocator: ^11.0.0
  intl: ^0.20.2

flutter_icons:
  android: true
  ios: true
  image_path: "web/Esta.png"

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  adaptive_icon_foreground: "web/Esta.png"
  adaptive_icon_background: "#000000"
  image_path_android: null

flutter:
  uses-material-design: true

  assets:
    - assets/image/
